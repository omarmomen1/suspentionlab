# Progress Update

**Last visited**: 2026-06-09T07:40:43+03:00

## Current Status
Investigating Alembic migration failures for `alembic upgrade head`.

## Findings
1. The project has two Alembic configs: `alembic.ini` (root) and `src/suspensionlab/backend/alembic.ini`. The root one is broken/outdated. The backend one should be used.
2. `alembic upgrade head` initially failed with "UNIQUE constraint failed: _alembic_tmp_sessions.id" because of a left-over/autogenerated `7174f75534bc_check.py` migration script which was improperly altering UUID columns on SQLite.
3. Deleting `7174f75534bc_check.py` allows `alembic upgrade head` to succeed.
4. When `alembic revision --autogenerate` is run, it generates false-positive alterations for all `UUID` columns from `NUMERIC` to `UUID`. This happens because SQLAlchemy reflects `UUID` types on SQLite as `NUMERIC`.
5. For the "tier system (Free, Pro, Enterprise)" part of the prompt, I noticed that `tier` was dropped in `8ff4d3e48527_drop_tier_column.py`, and `plan` is being used instead. I need to verify if there's any remaining schema mismatch related to tiers.

## Next Steps
1. Verify if `plan` vs `tier` causes any schema mismatch or if it's already completely fixed.
2. Recommend setting `compare_type=False` (default) and adding logic to ignore UUID type changes if `compare_type=True` is needed, or advise against blindly running `--autogenerate` without reviewing.
3. Check `tier` vs `plan` throughout the codebase to ensure synchronization is complete.
