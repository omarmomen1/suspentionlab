apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Release.Name }}-api-pdb
  namespace: suspensionlab-prod
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: suspensionlab
      component: api
