apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api
  namespace: suspensionlab-prod
spec:
  selector:
    app: suspensionlab
    component: api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
